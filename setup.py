from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='load mixed type pandas dataframes to gcp bigquery',
    author='chasf',
    license='',
)
